<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.mapper.DeptMapper">
    <!-- 查询所有部门数据 -->
    <select id="findAll" resultType="com.xuan.pojo.Dept">
        select id, name, create_time, update_time 
        from dept 
        order by update_time desc
    </select>

    <!-- 根据id删除部门 -->
    <delete id="deleteById">
        delete from dept where id = #{id}
    </delete>

    <!-- 新增部门 -->
    <insert id="insert" parameterType="com.xuan.pojo.Dept">
        insert into dept (name, create_time, update_time) 
        values (#{name}, #{createTime}, #{updateTime})
    </insert>

    <!-- 查询部门 -->
    <select id="getById" resultType="com.xuan.pojo.Dept">
        select id, name, create_time, update_time 
        from dept 
        where id=#{id}
    </select>

    <!-- 修改部门 -->
    <update id="update" parameterType="com.xuan.pojo.Dept">
        update dept 
        set name=#{name}, update_time=#{updateTime} 
        where id =#{id}
    </update>

    <!-- 检查部门是否有员工 -->
    <select id="countEmployeesByDeptId" resultType="integer">
        select count(*) from emp where dept_id = #{deptId}
    </select>
</mapper>